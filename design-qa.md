# 생태계 세부 지도 검증

- 확인일: 2026-09-14
- 구현: http://127.0.0.1:3000/ecosystem
- 시각 기준: 사용자가 유지하기로 한 기존 전체 지도의 캡처 [docs/qa/ecosystem-drilldown/ecosystem-before.jpg](docs/qa/ecosystem-drilldown/ecosystem-before.jpg). 세부 지도는 이 화면의 카드·색상·글꼴 체계를 확장한 새 화면이며 별도의 완성 시안은 없다.
- 결과 캡처: [전체 지도](docs/qa/ecosystem-drilldown/ecosystem-after.jpg), [MCP](docs/qa/ecosystem-drilldown/ecosystem-mcp.jpg), [TypeScript SDK](docs/qa/ecosystem-drilldown/ecosystem-typescript.jpg), [모바일 360](docs/qa/ecosystem-drilldown/ecosystem-mobile-360.jpg).

## 비교 조건과 근거

전체 지도 원본과 결과를 같은 비교 입력으로 열어 확인했다. 두 캡처 모두 `/ecosystem`, 다크 테마, 미선택, 스크롤 0, CSS 뷰포트 1440×1000, 이미지 1440×1000, 밀도 1이다. 브라우저 테두리를 제외한 페이지 캡처이며 크기 보정은 하지 않았다. 기존 제목·분야 그리드·카드·가이드의 배치와 스타일을 유지하고 가이드에 세부 탐색 진입부를 추가했다.

MCP와 TypeScript SDK 결과는 같은 1440×1000/밀도 1에서 세부 지도 진입 후 지도 상단으로 이동한 상태다. 모바일 결과는 360×800/밀도 1이며, 390×844와 데스크톱 1280 폭도 추가 확인했다. 모바일은 중심 카드를 먼저 보여주고 주변 그룹을 두 열로 배치한다. 서로 다른 반응형 배치를 원본과의 픽셀 불일치로 판정하지 않았다.

전체 화면 비교 외에 선택된 TypeScript SDK 카드 아래의 연결 설명·세부지도 버튼 영역을 집중 확인했다. [수정 전](docs/qa/ecosystem-drilldown/ecosystem-mcp-selected.jpg)과 [수정 후](docs/qa/ecosystem-drilldown/ecosystem-mcp-selected-fixed.jpg)를 같은 입력으로 비교했다. 두 캡처 모두 1440×1000, MCP 장면에서 TypeScript SDK 선택, 같은 스크롤 위치다. DOM 사각형 검사에서 표시된 관계 설명과 카드·버튼의 겹침은 0건이었다.

## 발견 사항과 수정 이력

| 우선순위 | 발견 사항과 영향 | 수정 및 후속 확인 |
|---|---|---|
| P2 · 해결 | 선택된 SDK의 관계 설명이 세부지도 버튼과 겹쳐 글자가 읽히지 않음 | 중앙 연결의 시작·종료점을 상하 방향으로 조정하고 그룹 제목·버튼을 설명 배치의 장애물로 처리. 위 수정 전후 캡처에서 설명이 빈 중앙 공간에 표시됨. 공간이 없는 관계 설명은 상세 패널에서 제공. |
| P2 · 해결 | 마지막 SDK 장면에서 진입 항목 없는 `EXPLORE DEEPER` 영역 노출 | 다음 장면이 있을 때만 영역 표시. 최종 TypeScript SDK 캡처에서 빈 영역이 제거됨. |
| P2 · 해결 | 전환 중 비활성 영역에 포커스를 보내 복귀 포커스가 누락될 수 있음 | 전환 상태 해제 후 다음 렌더에서 포커스 복원. 두 단계 진입 후 이전 카드와 모바일 상세 제목의 실제 포커스 확인. |
| P2 · 해결 | 복귀 시 상세 자동 포커스를 생략하면 Escape 처리도 생략됨 | 키 처리와 자동 포커스 조건을 분리. 데스크톱·모바일에서 복귀 후 Escape로 상세 닫기 확인. |

현재 남은 조치가 필요한 P0/P1/P2 발견 사항은 없다. 별도 검토에서도 기존 전체 화면의 시각적 회귀와 데스크톱·360 폭의 잘림이 발견되지 않았다.

## 필수 시각 영역

| 영역 | 확인 결과 |
|---|---|
| 글꼴·타이포그래피 | 기존 DM Sans 및 시스템 한국어 폴백을 유지. 제목/그룹명/항목명/설명의 위계를 확인했고 데스크톱 항목명 14px, 설명 12px로 가독성을 조정했다. 줄바꿈과 긴 이름의 카드 경계를 확인했다. |
| 간격·레이아웃 | 기존 전체 지도 그리드와 가이드 폭 유지. 세부 지도 중심·네 그룹·여백의 구분을 확인. 고정 헤더 아래에 복귀 경로가 남고 모바일 가로 넘침과 카드 잘림이 없다. |
| 색상·토큰 | 기존 어두운 배경과 카드·테두리·텍스트 색을 계승. MCP는 분홍, SDK는 파랑으로 장면 중심을 표시. 선택한 연결을 밝게 강조하고 그 외 연결은 낮은 불투명도로 유지. |
| 이미지·자산 품질 | 기존 모노그램과 아이콘 체계를 재사용했다. 기존 로고·이미지를 다른 자산으로 대체하지 않았다. 새 연결선은 확대·반응형 배치에 맞는 SVG로 렌더링한다. 캡처는 JPEG 근거이며 실제 UI는 텍스트와 벡터다. |
| 문구·콘텐츠 | 중심 포함 항목 수를 `지도 항목`으로 표현. 새 표시가 최근 출시가 아니라 이전 지도에서 새로 드러난 항목임을 설명. 실제 관계의 의미·방향·공식 근거를 상세 패널에 제공하며 개발용 설명을 사용자 흐름에 넣지 않았다. |

## 상호작용과 자동 검사

- 전체 지도의 한 번 클릭은 선택·관계 강조·상세 확인으로 유지.
- MCP 더블클릭 또는 세부지도 버튼으로 첫 장면에 진입하고 TypeScript SDK로 두 번째 장면에 진입.
- 두 번의 뒤로가기와 전체 지도 경로 복귀 확인. 기존 선택, 관계 19개, 스크롤 위치 740px, MCP 카드 포커스를 실제 브라우저에서 복원 확인.
- 360/390 폭에서 버튼 및 키보드 Enter로 진입, 상세 열기·닫기, 다음 단계와 복귀 확인. 상세가 새 지도를 덮지 않으며 본문 스크롤 잠금이 해제됨.
- 모바일 상세의 Tab/Shift+Tab 포커스 순환과 Escape 닫기 확인. 고정 복귀 버튼이 헤더 아래에 보임.
- 새로고침 후 최종 탐색에서 콘솔 오류·경고 없음.
- `npm test`: 27/27 통과. `npm run typecheck`, `npm run build`: 통과.

동작 줄이기는 CSS와 JavaScript에서 지원하며 코드 경로를 확인했다. 브라우저에서 해당 환경 설정을 강제로 바꾸는 검사는 수행하지 않았다. 실제 사용자를 대상으로 한 이해도 검증과 다른 브라우저 엔진 검증은 후속 범위다.

## 완료 체크

- [x] 기존 화면과 같은 조건으로 비교
- [x] 세부 지도와 선택 상태의 수정 후 캡처
- [x] 모바일·키보드·두 단계 복귀 검증
- [x] 자동 검사와 콘솔 확인
- [x] MCP → TypeScript SDK 시범 완료, 다른 대표 항목 확대는 후속 계획에 유지

final result: passed
